
mysql_config = {
  'user': 'root',
  'password': 'bd',
  'host': '127.0.0.1',
  'database': 'mobiway',
  # 'raise_on_warnings': True,
}

psql_config = {
  'user': 'postgres',
  'password': 'password',
  'host': '127.0.0.1',
  'database': 'mobiway_pgrouting',
}

