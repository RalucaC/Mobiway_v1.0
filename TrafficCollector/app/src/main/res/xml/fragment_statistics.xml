<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <EditTextPreference
        android:id="@+id/editTextMaxSpeed"
        android:key="@string/key_max_speed_preference"
        android:title="@string/max_speed"
        android:editable="false"
        android:clickable="false"
        android:enabled="false"
        />

    <EditTextPreference
        android:id="@+id/editTextMinSpeed"
        android:key="@string/key_min_speed_preference"
        android:title="@string/min_speed"
        android:editable="false"
        android:clickable="false"
        android:enabled="false"
        />

</PreferenceScreen>